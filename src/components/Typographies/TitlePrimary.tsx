import Typography from '@material-ui/core/Typography';
import withStyles from '@material-ui/core/styles/withStyles';

const TitlePrimary = withStyles({
    root: {
        fontSize: '2.625rem',
        lineHeight: '2.8125rem'
    }
})(Typography)

export default TitlePrimary
