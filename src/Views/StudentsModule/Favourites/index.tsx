import React, { FC } from 'react';
import FavouritesPageContainer from '../../../Containers/Pages/FavouritesPageContainer';

const Favourites: FC = () => {
    return <FavouritesPageContainer userType="students" />;
};

export default Favourites;
