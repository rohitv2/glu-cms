import React, { FC } from 'react';
import SuccessPageContainer from '../../../Containers/Pages/SuccessPageContainer';

const ClassCanceled: FC = () => {
    return <SuccessPageContainer userType="students" />;
};

export default ClassCanceled;
