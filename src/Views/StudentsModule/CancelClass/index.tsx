import React, { FC } from 'react';
import CancelClassPageContainer from '../../../Containers/Pages/CancelClassPageContainer';

const CancelClass: FC = () => {
    return <CancelClassPageContainer userType="students" />;
};

export default CancelClass;
