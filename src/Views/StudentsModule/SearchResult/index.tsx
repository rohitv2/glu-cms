import React, { FC } from 'react';
import SearchResultsPageContainer from '../../../Containers/Pages/SearchResultsPageContainer';

const SearchResult: FC = () => {
    return <SearchResultsPageContainer userType="students" />;
};

export default SearchResult;
