import React, { FC } from 'react';
import CalendarPageContainer from '../../../Containers/Pages/CalendarPageContainer';

const Calendar: FC = () => {
    return <CalendarPageContainer userType="students" />;
};

export default Calendar;
