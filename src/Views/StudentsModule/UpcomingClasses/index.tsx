import React, { FC } from 'react';
import UpcomingClassesPageContainer from '../../../Containers/Pages/UpcomingClassesPageContainer';

const UpcomingClasses: FC = () => {
    return <UpcomingClassesPageContainer userType="students" />;
};

export default UpcomingClasses;
