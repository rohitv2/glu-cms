import React, { FC } from 'react';
import HelpSupportPageContainer from '../../../Containers/Pages/HelpSupportPageContainer';

const HelpSupport: FC = () => {
    return <HelpSupportPageContainer userType="students" />
}

export default HelpSupport
