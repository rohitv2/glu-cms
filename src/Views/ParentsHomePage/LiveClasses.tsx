import React, { FC } from 'react';
import LiveClassesPageContainer from '../../Containers/Pages/LiveClassesPageContainer';

const LiveClasses: FC = () => {
    return (
        <LiveClassesPageContainer userType="parent" />
    )
}

export default LiveClasses
