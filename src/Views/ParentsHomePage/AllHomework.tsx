import React, { FC } from 'react';
import HomeworkContainer from '../../Containers/Pages/HomeworkContainer';

const LiveClasses: FC = () => {
    return (
        <HomeworkContainer userType="parent" />
    )
}

export default LiveClasses
